// mixed data - key, value

// when we use tuple type we have to decide what we use first "string" or "number"


let user: [string, number];


user = [520, "imran"];
user = ["imran", 521];


console.log(user);